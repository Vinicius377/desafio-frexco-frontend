import { createTheme } from "@mui/system";

const theme = createTheme({
  palette: {
    button: {
      green: "#2CB129",
      red: "#EC4040",
      blue: "#71FFFF",
    },
    box: {
      primary: "#F1F1F1",
    },
  },
});

export default theme;
